<UserControl x:Class="PEExplorer.Views.Tabs.DependenciesTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PEExplorer.Views.Tabs"
				 xmlns:tabs="clr-namespace:PEExplorer.ViewModels.Tabs"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition Width="2*" />
		</Grid.ColumnDefinitions>
		<Grid.Resources>
			<HierarchicalDataTemplate DataType="{x:Type tabs:DependencyTreeItem}" ItemsSource="{Binding Items}">
				<StackPanel Orientation="Horizontal">
					<Image Source="{Binding Icon}" Width="20" Height="20" />
					<TextBlock Text="{Binding Text}" VerticalAlignment="Center" Margin="4,0,0,0" />
				</StackPanel>
			</HierarchicalDataTemplate>
		</Grid.Resources>
		<TreeView>
			<TreeViewItem Header="{Binding PEImage}" ItemsSource="{Binding Dependencies}" />
		</TreeView>
		<Grid Grid.Column="1">
			
		</Grid>
		<GridSplitter Grid.Column="0" HorizontalAlignment="Right" Width="3" Margin="-1,0" VerticalAlignment="Stretch" Background="{DynamicResource AccentColorBrush}" Cursor="SizeWE" />
	</Grid>
</UserControl>
